[project]
name = "doc2substack"
version = "0.1.0"
description = "Convert LaTeX or Markdown documents to Substack-friendly HTML"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
doc2substack = "doc2substack:main"
